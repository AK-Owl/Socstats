 <h5>Data Analysis</h5>
 <section id="about" class="about-section text-center">
    <div class="container">
      <div class="row">
        
        <%= form_tag developments_path, :method => :get do  %>
          <div class="input-append">
            <%= collection_select :country, :id1, Development.all, :id, :country %>
            <%= collection_select :country, :id2, Development.all, :id, :country %>
            <%= collection_select :country, :id3, Development.all, :id, :country %>
            <%= collection_select :country, :id4, Development.all, :id, :country %>
            <%= collection_select :country, :id5, Development.all, :id, :country %>
          <button type="submit" class="btn">submit</button>
          </div>
        <% end %>
      </div>
    </div>
  </section>



